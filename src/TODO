TODO:
        refactor parses stop from smaller value to bigger

        add value module in install README

        return -1 in mode if eof

        add goto to clean before return, add exit_status

        Improve README

        Change current in node
        Delete node: user enter the value and we delete node with this value

----------------------------------------------------------------------------
Maybe never to do:

        Add in current prev or next pointer for single&doubly-linked lists,
        add current also change current pointer

        Improve chcur and dsp_cur funcs: remove workaround from chcur_single
