TODO:

translate program
add INPUTFILE for xgettext
add .gitignore

in README ./build.sh -> shell build.sh
improve build.sh: update translation files

PROGRAM -> PACKAGE
remove my email from this package
-v -> -V
if !res then goto quit in main.c

simplify headers name
move error`s messages in error.h and print them in main.c
add comments in all files
